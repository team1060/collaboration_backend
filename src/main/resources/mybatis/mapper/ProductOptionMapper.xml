<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team1060.golf.product.mapper.ProductOptionMapper">
	<insert id="insertProductOption">
		INSERT INTO tbl_p_option (product_no, name, count)
	    VALUES (#{product_no}, #{name}, #{count}); 
	</insert>
	
</mapper>